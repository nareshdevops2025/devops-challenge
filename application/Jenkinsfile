pipeline {
  agent any

    tools {
        maven 'Maven'
        jdk 'JDK'
    }
stages
{
 stage('Debug') {
    steps {
        sh '''
        uname -a
        mvn -v
        java -version
        '''
    }
}
}
}