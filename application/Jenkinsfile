pipeline {
  agent any

    tools {
        maven 'maven-3'
        jdk 'JDK'
    }
stages
{
 stage('Debug') {
    steps {
        sh '''
        uname -a
        mvn -v
        java -version
        '''
    }
}
}
}